"use strict"; // https://javascript.info/strict-mode

/*******************************************************************************
 * User Interaction
 *
 * JavaScript can interact with the user through `alert`, `prompt`, and `confirm`.
 * These functions are part of the browser's BOM (Browser Object Model) and are not available in Node.js.
 *******************************************************************************/
console.log("================================== User Interaction ==================================");

// The following lines are commented out to prevent blocking the script execution.
// You can uncomment them to see how they work in a browser environment.

/*
// alert: shows a message.
alert("Hello from alert!");

// prompt: shows a message asking the user for input.
let name = prompt("What is your name?", "");
alert(`Your name is: ${name}`);

// confirm: shows a message and waits for the user to press "OK" or "Cancel".
let isBoss = confirm("Are you the boss?");
alert(isBoss); // true if OK is pressed, false otherwise.
*/
